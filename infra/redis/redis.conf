# AZTDP Redis configuration — development
bind 0.0.0.0
port 6379

# No password in dev; set requirepass in production
# requirepass <your-password>

maxmemory 256mb
maxmemory-policy allkeys-lru

# Disable persistence in dev to keep startup fast
save ""
appendonly no

loglevel notice
